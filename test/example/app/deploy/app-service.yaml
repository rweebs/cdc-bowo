apiVersion: v1
kind: Service
metadata:
  name: app
spec:
  type: NodePort
  ports:
    - name: app
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: green
    tier: backend